% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rFAMS-internals.R
\name{iCheckSlotType}
\alias{iCheckSlotType}
\title{Make checks of combinations of \code{cf} values and \code{recruitmentTL} for slot limits}
\usage{
iCheckSlotType(cfu, cfi, cfa, rtl, strict = TRUE)
}
\arguments{
\item{cfu}{A \code{cfBelow} value.}

\item{cfi}{A \code{cfIn} value.}

\item{cfa}{A \code{cfAbove} value.}

\item{rtl}{A \code{recruitmentTL} value.}

\item{strict}{A logical that indicates how strict the test should be. See details.}
}
\description{
Make checks of combinations of \code{cf} values and \code{recruitmentTL} for slot limits
}
\details{
\code{strict} is a logical that indicates whether strict criterion for values of \code{recruitmentTL}, \code{cfBelow}, \code{cfIn}, and \code{cfAbove} should be used. If \code{strict=TRUE} then the only accepted combinations are that a \code{recruitmentTL} is given (i.e., not \code{NULL}), \code{cfBelow}>0, \code{cfAbove}>0, and \code{cfIn}=0 (i.e., simulating a protected slot) or \code{recruitmentTL} is \code{NULL}, \code{cfBelow}=0, \code{cfAbove}=0, and \code{cfIn}>0 (i.e., simulating an inverse/harvest slot). If \code{strict=FALSE} then the only restrictions are that the three \code{cf}s cannot all =0, and that if \code{cfBelow} is given them \code{recruitmentTL} cannot be \code{NULL}. \strong{This argument allows us to model each type of restrictions while we ultimately decide which one to use.}
}
\keyword{internal}
